/**
 * generate the path to the docs file for the rule
 * @param name the rule's name
 * @returns a URL to the doc path
 */
export const DOC_PATH = (name: string) =>
  `https://github.com/bailey-byb/eslint-plugin-avoid-jest-spy-on/blob/main/docs/rules/${name}.md`;
